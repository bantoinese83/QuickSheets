# QuickSheets Backend – copy to .env and fill in.
# Full reference: docs/ENV.md. Problems? See docs/TROUBLESHOOTING.md.

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
# Port the server listens on. Change if 3000 is in use.
PORT=3000
# Exact origin of the Excel add-in (no trailing slash). Wrong = CORS errors.
CORS_ORIGIN=https://localhost:3000

# -----------------------------------------------------------------------------
# Database (required)
# -----------------------------------------------------------------------------
# Postgres connection string. Create DB first, then: psql "$DATABASE_URL" -f db/schema.sql
DATABASE_URL=postgresql://user:password@localhost:5432/quicksheets

# -----------------------------------------------------------------------------
# Token encryption (optional in dev)
# -----------------------------------------------------------------------------
# 32-byte hex or any string (hashed to key). Omit = tokens stored plaintext (dev only).
# Set in production so QBO tokens are encrypted at rest.
# ENCRYPTION_KEY=

# -----------------------------------------------------------------------------
# QuickBooks Online (required for Connect QuickBooks + real reports)
# -----------------------------------------------------------------------------
# Create app: https://developer.intuit.com → Keys & credentials.
# Add Redirect URI in the app to match QBO_REDIRECT_URI exactly.
QBO_CLIENT_ID=
QBO_CLIENT_SECRET=
# Must match the redirect URI in the Intuit app (scheme, host, port, path).
QBO_REDIRECT_URI=http://localhost:3000/auth/qbo/callback
# true = sandbox; false = production QBO.
QBO_USE_SANDBOX=true

# -----------------------------------------------------------------------------
# Stripe billing (optional; omit to skip subscription check)
# -----------------------------------------------------------------------------
# If set, POST /api/refresh returns 402 unless user has active/trialing subscription.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=
STRIPE_SUCCESS_URL=https://your-domain.com?subscription=success
STRIPE_CANCEL_URL=https://your-domain.com?subscription=canceled

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------
# If set to true, POST /api/refresh returns demo data when user has no QBO connection.
# USE_DEMO_DATA=true
